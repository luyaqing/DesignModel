迭代器模式
    是一种没落的模式，基本上没人会单独写一个迭代器，除非是产品性质的开发、
    提供了一种方法访问一个容器对象中各个元素，而又不需要暴露该对象的内部细节。

迭代器是为容器服务的

    在实现中，不一定非要实现Java的Iterator接口
    自己写一个这个接口也是可以的，使用起来还比较方便；存在三个方法  next() 、 hasNext()、remove()。

迭代器类似于一个数据库中的游标，可以在一个容器内上下翻滚，遍历所有他需要查看的元素